# Copy this content to a .env file in the backend directory

# Database Configuration
DATABASE_URL="mysql://root:password@localhost:3306/lanmic_db"

# JWT Configuration (works in both development and production)
# Note: Change these secrets when deploying to production for security
JWT_SECRET="dev-jwt-secret-key-12345"
ACCESS_TOKEN_EXPIRY="15m"
REFRESH_TOKEN_EXPIRY="7d"

# Application Configuration
PORT=3002
NODE_ENV=development
FRONTEND_URL="http://localhost:3000"
